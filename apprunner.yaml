version: 1.0
runtime: python3
build:
  commands:
    - pip install requests
run:
  command: ["python3", "-u", "service.py"]
  network:
    port: 5714
  env:
    - name: TZ
      value: America/New_York
  filesystem:
    mount_points:
      - path: /plugin.video.mlb/data
        source: ./data
